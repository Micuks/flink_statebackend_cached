<?xml version="1.0" encoding="UTF-8"?>
<!--
Licensed to the Apache Software Foundation (ASF) under one
or more contributor license agreements.  See the NOTICE file
distributed with this work for additional information
regarding copyright ownership.  The ASF licenses this file
to you under the Apache License, Version 2.0 (the
"License"); you may not use this file except in compliance
with the License.  You may obtain a copy of the License at

  http://www.apache.org/licenses/LICENSE-2.0

Unless required by applicable law or agreed to in writing,
software distributed under the License is distributed on an
"AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY
KIND, either express or implied.  See the License for the
specific language governing permissions and limitations
under the License.

-->
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">

	<modelVersion>4.0.0</modelVersion>

	<parent>
		<groupId>org.apache.flink</groupId>
		<artifactId>flink-state-backends</artifactId>
		<version>1.16-SNAPSHOT</version>
	</parent>

	<artifactId>flink-statebackend-cachekit</artifactId>
	<name>Flink : State backends : CacheKit</name>

	<packaging>jar</packaging>

	<dependencies>
		<!-- core dependencies -->
		<dependency>
			<groupId>org.apache.flink</groupId>
			<artifactId>flink-runtime</artifactId>
			<version>${project.version}</version>
			<scope>provided</scope>
		</dependency>

		<dependency>
			<groupId>org.apache.flink</groupId>
			<artifactId>flink-core</artifactId>
			<version>${project.version}</version>
			<scope>provided</scope>
		</dependency>

		<dependency>
			<groupId>org.apache.flink</groupId>
			<artifactId>flink-statebackend-rocksdb</artifactId>
			<version>${project.version}</version>
			<scope>provided</scope>
		</dependency>

		<dependency>
			<groupId>org.apache.flink</groupId>
			<artifactId>flink-table-common</artifactId>
			<version>${project.version}</version>
			<scope>provided</scope>
		</dependency>

		<dependency>
			<groupId>com.github.ben-manes.caffeine</groupId>
			<artifactId>caffeine</artifactId>
			<version>3.1.8</version>
		</dependency>

		<dependency>
			<groupId>it.unimi.dsi</groupId>
			<artifactId>fastutil</artifactId>
			<version>8.5.12</version>
		</dependency>

		<!--
		Declared so the patched sibling modules build before this one: the shaded
		artifact overlays a handful of their classes (see maven-dependency-plugin
		below) so that dropping this single jar into flink/lib/ is enough.
		-->
		<dependency>
			<groupId>org.apache.flink</groupId>
			<artifactId>flink-streaming-java</artifactId>
			<version>${project.version}</version>
			<scope>provided</scope>
		</dependency>

		<dependency>
			<groupId>org.apache.flink</groupId>
			<artifactId>flink-table-runtime</artifactId>
			<version>${project.version}</version>
			<scope>provided</scope>
		</dependency>


		<!-- test dependencies -->
		<dependency>
			<groupId>org.apache.flink</groupId>
			<artifactId>flink-test-utils-junit</artifactId>
			<scope>test</scope>
		</dependency>

		<dependency>
			<groupId>org.apache.flink</groupId>
			<artifactId>flink-table-runtime</artifactId>
			<version>${project.version}</version>
			<scope>test</scope>
		</dependency>

		<dependency>
			<groupId>org.mockito</groupId>
			<artifactId>mockito-junit-jupiter</artifactId>
			<version>${mockito.version}</version>
			<scope>test</scope>
		</dependency>
	</dependencies>

	<build>
		<plugins>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-checkstyle-plugin</artifactId>
				<configuration>
					<skip>true</skip>
				</configuration>
			</plugin>
			<plugin>
				<groupId>com.diffplug.spotless</groupId>
				<artifactId>spotless-maven-plugin</artifactId>
				<configuration>
					<skip>true</skip>
				</configuration>
			</plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <source>11</source>
                    <target>11</target>
                </configuration>
            </plugin>

            <!--
            Single-jar packaging. CacheKit needs a few classes that live in
            flink-streaming-java, flink-statebackend-rocksdb and
            flink-table-runtime: some are new (batch/prefetch plumbing), some
            override an upstream class with an additive hook. Rather than asking
            operators to drop a patched flink-dist plus several jars into
            flink/lib/, those classes are copied into this artifact. Flink sorts
            lib/*.jar and appends flink-dist last, so this jar (f...) precedes
            flink-table-runtime (flink-t...) and flink-dist, and first-wins
            class loading makes the overrides take effect.
            -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-dependency-plugin</artifactId>
                <executions>
                    <execution>
                        <id>overlay-patched-classes</id>
                        <phase>process-classes</phase>
                        <goals>
                            <goal>unpack</goal>
                        </goals>
                        <configuration>
                            <!-- do not clobber CacheKit's own freshly compiled classes -->
                            <overWriteReleases>false</overWriteReleases>
                            <overWriteSnapshots>false</overWriteSnapshots>
                            <outputDirectory>${project.build.outputDirectory}</outputDirectory>
                            <artifactItems>
                                <artifactItem>
                                    <groupId>org.apache.flink</groupId>
                                    <artifactId>flink-streaming-java</artifactId>
                                    <version>${project.version}</version>
                                    <includes>
                                        org/apache/flink/streaming/api/operators/BatchProcessingOperator*.class,
                                        org/apache/flink/streaming/api/operators/BatchableKeyedFunction*.class,
                                        org/apache/flink/streaming/api/operators/CommutativeKeyedOperator*.class,
                                        org/apache/flink/streaming/runtime/io/BatchOutput*.class,
                                        org/apache/flink/streaming/runtime/io/CollapseProbe*.class,
                                        org/apache/flink/streaming/runtime/io/LocalPreagg*.class,
                                        org/apache/flink/streaming/runtime/io/StreamRecordBatchOutput*.class,
                                        org/apache/flink/streaming/runtime/io/StreamOneInputProcessor*.class,
                                        org/apache/flink/streaming/runtime/tasks/BatchedKeyedOperatorAdapter*.class,
                                        org/apache/flink/streaming/runtime/tasks/OneInputStreamTask*.class,
                                        org/apache/flink/streaming/runtime/tasks/StatePrefetcher*.class
                                    </includes>
                                </artifactItem>
                                <artifactItem>
                                    <groupId>org.apache.flink</groupId>
                                    <artifactId>flink-statebackend-rocksdb</artifactId>
                                    <version>${project.version}</version>
                                    <includes>
                                        org/apache/flink/contrib/streaming/state/AbstractRocksDBState*.class,
                                        org/apache/flink/contrib/streaming/state/RocksDBBatchValueReader*.class,
                                        org/apache/flink/contrib/streaming/state/RocksDBConfigurableOptions*.class,
                                        org/apache/flink/contrib/streaming/state/RocksDBResourceContainer*.class,
                                        org/apache/flink/contrib/streaming/state/RocksDBValueState*.class
                                    </includes>
                                </artifactItem>
                                <artifactItem>
                                    <groupId>org.apache.flink</groupId>
                                    <artifactId>flink-table-runtime</artifactId>
                                    <version>${project.version}</version>
                                    <includes>
                                        org/apache/flink/table/runtime/operators/aggregate/GroupAggFunction*.class,
                                        org/apache/flink/table/runtime/operators/deduplicate/RowTimeDeduplicateFunction*.class
                                    </includes>
                                </artifactItem>
                            </artifactItems>
                        </configuration>
                    </execution>
                </executions>
            </plugin>

            <!--
            Bundle (and relocate) the only two non-Flink runtime dependencies so
            the jar is self-contained: without this, selecting the Caffeine cache
            policy or the primitive presence cache would NoClassDefFoundError.
            -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-shade-plugin</artifactId>
                <executions>
                    <execution>
                        <id>shade-cachekit-runtime-deps</id>
                        <phase>package</phase>
                        <goals>
                            <goal>shade</goal>
                        </goals>
                        <configuration>
                            <shadedArtifactAttached>false</shadedArtifactAttached>
                            <createDependencyReducedPom>false</createDependencyReducedPom>
                            <!--
                            fastutil ships a class per primitive combination
                            (~13k, 26 MB). Bytecode-reachability pruning keeps
                            only the closure CacheKit actually reaches. Caffeine
                            is exempted below because it loads its internal
                            cache/node implementations reflectively by name, so
                            pruning would break it at runtime.
                            -->
                            <minimizeJar>true</minimizeJar>
                            <artifactSet>
                                <includes>
                                    <include>com.github.ben-manes.caffeine:caffeine</include>
                                    <include>it.unimi.dsi:fastutil</include>
                                </includes>
                            </artifactSet>
                            <relocations>
                                <relocation>
                                    <pattern>com.github.benmanes.caffeine</pattern>
                                    <shadedPattern>org.apache.flink.contrib.streaming.state.cachekit.shaded.caffeine</shadedPattern>
                                </relocation>
                                <relocation>
                                    <pattern>it.unimi.dsi.fastutil</pattern>
                                    <shadedPattern>org.apache.flink.contrib.streaming.state.cachekit.shaded.fastutil</shadedPattern>
                                </relocation>
                            </relocations>
                            <filters>
                                <filter>
                                    <artifact>*:*</artifact>
                                    <excludes>
                                        <exclude>META-INF/*.SF</exclude>
                                        <exclude>META-INF/*.DSA</exclude>
                                        <exclude>META-INF/*.RSA</exclude>
                                        <exclude>module-info.class</exclude>
                                    </excludes>
                                </filter>
                                <!--
                                fastutil is pruned by minimizeJar above; only
                                the reachable closure survives.
                                -->
                                <!-- Caffeine resolves its internal implementations by name -->
                                <filter>
                                    <artifact>com.github.ben-manes.caffeine:caffeine</artifact>
                                    <includes>
                                        <include>**</include>
                                    </includes>
                                </filter>
                            </filters>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
	</build>

	<profiles>
		<profile>
			<id>cachekit-native-linux-aarch64</id>
			<activation>
				<os>
					<name>Linux</name>
					<arch>aarch64</arch>
				</os>
			</activation>
			<build>
				<plugins>
					<plugin>
						<groupId>org.apache.maven.plugins</groupId>
						<artifactId>maven-antrun-plugin</artifactId>
						<executions>
							<execution>
								<id>build-cachekit-native-snapshot</id>
								<phase>generate-resources</phase>
								<goals>
									<goal>run</goal>
								</goals>
								<configuration>
									<target>
										<exec executable="cmake" failonerror="true">
											<arg value="-S"/>
											<arg value="${project.basedir}/src/native/snapshot-cache"/>
											<arg value="-B"/>
											<arg value="${project.build.directory}/native-snapshot"/>
											<arg value="-DCMAKE_BUILD_TYPE=Release"/>
										</exec>
										<exec executable="cmake" failonerror="true">
											<arg value="--build"/>
											<arg value="${project.build.directory}/native-snapshot"/>
											<arg value="--parallel"/>
										</exec>
										<mkdir dir="${project.build.outputDirectory}/META-INF/native"/>
										<copy file="${project.build.directory}/native-snapshot/libcachekit_snapshot_jni.so"
											  tofile="${project.build.outputDirectory}/META-INF/native/libcachekit_snapshot_jni.so"/>
									</target>
								</configuration>
							</execution>
						</executions>
					</plugin>
				</plugins>
			</build>
		</profile>
	</profiles>

</project>
