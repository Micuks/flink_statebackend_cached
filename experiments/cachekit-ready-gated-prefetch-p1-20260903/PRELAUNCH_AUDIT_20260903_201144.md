# P1 prelaunch audit

Recorded at `2026-09-03T20:11:44+08:00`.

## Verdict

Implementation and frozen canary inputs: **PASS**.
Remote launch: **WAITING_FOR_PARENT_CAMPAIGN**.

## Runtime and artifact

- Base source: `7a9e568dcbd75ab4f24e7a33a4339f5cd1cf8000`
- P1 runtime source: `5a9d1e656715a403afac72ee1a516876ddbfb7f1`
- Candidate SHA-256:
  `58676b125fe20a5e7f9994e4531f06f8f076e1b3fc150ee1f222fabbe4e20602`
- Candidate size: `3,104,528` bytes
- Frozen overlay SHA-256:
  `869ec0e983dfb2a4e35b93ebfe9d50f5595757d7cebd94a4c14a2d5b58d2ed5e`
- Independent byte comparison: all 26 current streaming overlay classes and all
  9 selected frozen RocksDB/table classes match their declared inputs.
- Runtime changes are confined to CacheKit and streaming modules; subsequent
  experiment commits do not change those runtime trees.

The first independent audit rejected the inherited runtime manifest because it
incorrectly left `flink-streaming-java` under `flink_dist`. This was fixed:
`flink_dist` now covers `flink-statebackend-rocksdb`, while the candidate covers
`flink-statebackend-cachekit` and `flink-streaming-java`. Staging runs the frozen
`runtime_bundle.py` validator and persists `P1_RUNTIME_BUNDLE_AUDIT.json` before
launch. Independent re-audit returned **PASS**.

## Verification

- Streaming focused suite: 32 tests, 0 failures/errors/skips; Checkstyle and
  Spotless pass.
- CacheKit completion/lifecycle suite: 57 tests, 0 failures/errors/skips.
- Config audit: exactly one difference, ready-gated `false` versus `true`.
- Campaign arrays are hardcoded to q9, R1, `control ready-d2`; no environment
  override can silently change the frozen identity.
- Stage guard test: exit 70 while the parent PID is live; no target directory
  was created.

## External occupancy

At capture time, parent PID `3253969` was alive, `R3_COMPLETE` was absent, and
five `ckx86bbd39abl` containers were running. Its current leg was
`090-r2-q20-stage1`. No parent container/process/file was modified.

P1 staging requires the parent PID to exit, `R3_COMPLETE` to exist, and the host
to have zero running containers. Once true, run:

```bash
LAUNCH=1 experiments/cachekit-ready-gated-prefetch-p1-20260903/stage_x86.sh \
  flink-state-backends/flink-statebackend-cachekit/target/flink-statebackend-cachekit-1.16-SNAPSHOT.jar
```

After both legs complete, copy the remote evidence, run `summarize.py`, and
advance only if activation passes and q9 K/s/core uplift is at least `+3.00%`.
